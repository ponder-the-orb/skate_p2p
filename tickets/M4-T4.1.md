TICKET M4-T4.1 — CI: nobody merges red again
GOAL: Every push and PR runs analyze, format, the full test suite,
and the relay smoke test on GitHub's runners, so red is visible
before any merge click. Proven to bite, not just to pass.
BRANCH: m4/ci — cut from m4/relay-url, NOT main: PR #14 is
verdict-MERGE but unmerged, and both tickets write CHANGELOG
[Unreleased]; stacking avoids that collision. Set the PR base to
m4/relay-url and say so in FLAGS; the Producer retargets to main
after #14 lands. This ticket file is the branch's first commit
(ARCHITECT §3, Remote Producer).
FILES:
  - tickets/M4-T4.1.md (new — this file)
  - .github/workflows/ci.yml (new)
  - CHANGELOG.md [Unreleased] · reports/M4-T4.1.md (new)
  - plus ONE throwaway branch, m4/ci-bite-check, cut from m4/ci:
    one commit adding a deliberately failing test. Never opened as
    a PR, never merged; the Producer deletes it at the next sweep.
SPEC: ROADMAP.md M4 row T4.1; DEV_SETUP.md "Tests" (those four
commands are the contract); WORKFLOW.md §7 (built-in GITHUB_TOKEN
only, no new secrets).
NOTES (Architect rulings):
  - Two parallel jobs, so a relay-only failure reads as one.
    flutter job: subosito/flutter-action pinned to an EXACT
    flutter-version — never channel:stable; CI must not go red on a
    Flutter release that isn't our diff. Note the chosen version in
    the report. cache:true, then flutter pub get → flutter analyze →
    dart format --output=none --set-exit-if-changed . → flutter test.
    relay job: actions/setup-node (node 20) →
    node skate_signaling_server/test/rooms_smoke.js (self-contained:
    own relay on 8129, GRACE_MS=200 — runner-safe).
  - Triggers: on push (all branches) + pull_request, with a
    concurrency group per ref and cancel-in-progress: true. Public
    repo, minutes are free; cancelling dupes keeps Actions legible.
  - No README badge (T4.5), no apk build (slow, out of scope), no
    secrets of any kind.
  - PUSH TRAP: pushing workflow files over HTTPS needs a token with
    workflow scope; this repo pushes over SSH, which doesn't care.
    If the push is rejected mentioning workflow scope, report
    BLOCKED — do not fiddle with auth.
  - THE trap this ticket exists for: a green run that ran nothing
    (wrong dir, filtered branch, skipped tests). That is why
    acceptance is asymmetric — one green AND one red.
  - Bite check: from m4/ci cut m4/ci-bite-check, add
    test/ci_bite_check_test.dart containing a single
    expect(true, false), push. Confirm the red run with
    gh run list / gh run watch if gh cooperates; either way, put
    the Actions links for BOTH runs in the report.
ACCEPTANCE:
  - Local checks green before every commit on m4/ci, as always
  - Actions: m4/ci run green · m4/ci-bite-check run red · both
    links in reports/M4-T4.1.md (Producer eyeballs from a phone)
  - CHANGELOG under [Unreleased] · five-line report · PR link
OUT OF SCOPE: on m4/ci, zero diff outside .github/, tickets/,
reports/, CHANGELOG.md — lib, test, server, docs, pubspec all
untouched. The failing test exists only on the throwaway branch.
IF IT BALLOONS: land the flutter job alone, green + red proven,
DONE-PARTIAL; the relay job becomes T4.1b.
