TICKET M3-T3.5 — Letter tracks fit narrow screens
GOAL: No horizontal overflow on A14-class widths. Kill the striped bars.
BRANCH: m3/letter-fit — push, open PR, never touch main.
FILES: lib/ui/screens/match_screen.dart (letter-track widgets only)
       test/match_screen_test.dart (extend)
SPEC: STRATEGY.md ("jank is unforgivable"); ROADMAP M3.
NOTES (Architect rulings):
  - Debug shows "RIGHT OVERFLOWED BY 10.0 PIXELS" on BOTH tracks at
    ~360–412 logical px; release builds clip silently instead.
  - Fix is implementer's choice: FittedBox(fit: BoxFit.scaleDown) around
    each track's Row, or tile sizes derived via LayoutBuilder. No new
    deps; design unchanged at normal widths; the newest-letter emphasis
    (filled red, slightly larger) must survive the fix.
  - Tests: set tester.view.physicalSize + devicePixelRatio (reset in
    tearDown) to pump at 320-wide and 360-wide logical; a RenderFlex
    overflow raises FlutterError in tests, so passing == fixed. Cover
    letters at 0/0, 3/2, and 5/4.
ACCEPTANCE: narrow-width tests green · full suite, analyze, format
  green · CHANGELOG · reports/M3-T3.5.md · PR link. Manual: A14 debug
  build shows clean tracks, no stripes.
OUT OF SCOPE: zero diff outside the two files.
IF IT BALLOONS: it can't. If it tries, stop and report.
