TICKET M3-T3.1 — Preset trick list
GOAL: Setters can tap a common trick instead of typing it. Free text
stays. No wire, engine, or server changes — presets are UI convenience.
BRANCH: m3/trick-presets — push, open PR, never touch main.
FILES:
  - new lib/ui/data/trick_presets.dart
  - lib/ui/screens/match_screen.dart (state 1 setter panel only)
  - test/match_screen_test.dart (extend state 1)
  - new test/trick_presets_test.dart
SPEC: ROADMAP.md M3; ARCHITECTURE.md §3 (ui renders + emits intents,
nothing else); PROTOCOL.md §6 (a trick name is 0–254 UTF-8 bytes).
NOTES (Architect rulings):
  - trick_presets.dart: one `const List<String> trickPresets`, ~15–25
    common street tricks ordered roughly easy → hard (Ollie, Kickflip,
    Heelflip, Pop Shove-it, FS 180, BS 180, … implementer's list; PG
    names, no jokes). Lives in ui/ because it's picker content, not a
    rule.
  - UX: horizontally scrollable chip row above the trick text field,
    visible only in state 1 (I'm setter, nothing declared). Tapping a
    chip FILLS the field (still editable); SET stays the single commit
    path. Chips never declare directly.
  - Existing tests keep passing, modified only where they assert the
    setter panel's children.
ACCEPTANCE:
  - trick_presets_test: non-empty, no empty or duplicate entries, and
    every name ≤ 254 bytes UTF-8 encoded — the §6 wire limit. Even a
    dumb list has one wire-derived constraint; the test pins it.
  - match_screen_test: chips render in state 1; tapping one fills the
    field; SET after a chip tap declares that exact name (assert via
    app.game.currentTrickName).
  - flutter analyze clean · flutter test green · format check passes
  - CHANGELOG under [Unreleased] · five-line report + PR link
OUT OF SCOPE: categories/search/favorites, timers, reconnect, camera,
any engine/net/server/protocol diff (must be zero).
IF IT BALLOONS: it shouldn't — if it does, stop and report; something
is wrong with the approach.
