REPORT M4-T4.0 — DONE
CHANGED: lib/main.dart (+10/-2) · test/relay_url_test.dart (+11/-0, new) · docs/DEV_SETUP.md (+14/-5) · docs/ROADMAP.md (+7/-3) · docs/ARCHITECT.md (+5/-1) · CHANGELOG.md (+8/-0) · tickets/M4-T4.0.md (+86/-0, new) · reports/M4-T4.0.md (+12/-0, new)
CHECKS: analyze ✅  test ✅ (172 passed, +1)  format ✅  rooms_smoke ➖ (relay untouched)
COMMITS: docs(tickets): add M4-T4.0 · feat(net): choose the relay per build via --dart-define=RELAY_URL · docs: DEV_SETUP covers the RELAY_URL build flag · docs: M3 status in ROADMAP; ARCHITECT amendments · docs: changelog + report for M4-T4.0
CHANGELOG: updated
QUESTIONS/FLAGS:
- Ticket-as-first-commit, per the ticket's own BRANCH line (Remote Producer): `tickets/M4-T4.0.md` is commit 1 on `m4/relay-url` rather than landing on `main` beforehand. Branch was cut from a freshly pulled `main` (1ca34c7).
- Working-tree leftovers left untouched exactly as instructed — stash@{0}, `project_snapshot.md`, `reports/Report-M*.md`, `tickets/M1-*`/`M2-*`/`M3-*.md`, and the modified `docs/GIT_SURVIVAL_GUIDE.md` are all still uncommitted. Every commit staged files by name; no `git add -A`.
- The ROADMAP Acceptance sentence is the Architect's text verbatim, rewrapped at the file's ~78-column margin so the paragraph doesn't grow one long line. Wording unchanged.
- The doc comment on `relayUrl` uses `ws://192.168.1.20:8080` as its one `--dart-define` example (the ticket asked for an example but didn't dictate the address); DEV_SETUP uses the ticket's literal `ws://<laptop-ip>:8080` placeholder.
- Manual acceptance (laptop `PORT=9000` run, and the two-phone hotspot game) is the Producer's — not run here.
DIFF: https://github.com/ponder-the-orb/skate_p2p/pull/14
