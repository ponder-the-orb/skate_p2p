REPORT M4-T4.1 — DONE
CHANGED: .github/workflows/ci.yml +56 (new) · CHANGELOG.md +9 · tickets/M4-T4.1.md +57 (new) · reports/M4-T4.1.md +24 (new)
CHECKS: analyze ✅  test ✅ (172 passed)  format ✅  rooms_smoke ✅ — locally before every commit, and again on GitHub's runners
COMMITS: docs: ticket M4-T4.1 (CI) · ci: analyze, format, tests and the relay smoke test on every push and PR · ci: pin checkout and setup-node to v5; changelog for M4-T4.1 · docs: report for M4-T4.1
CHANGELOG: updated under [Unreleased]
QUESTIONS/FLAGS:
  - PR BASE IS m4/relay-url, NOT main, as the ticket directs (PR #14 unmerged, both tickets write CHANGELOG [Unreleased]). Producer retargets to main after #14 lands.
  - Flutter pinned to 3.47.0 — the exact version on this dev box (Dart 3.13.0), matching pubspec's `sdk: ^3.13.0`. `channel: stable` is still passed alongside it because subosito/flutter-action needs a channel to resolve the release manifest; the exact `flutter-version` is what actually selects the SDK, so a new stable release cannot move CI.
  - ADDED A STEP THE TICKET DID NOT LIST: `npm ci` in `skate_signaling_server/` before the smoke test. The suite is self-contained in the sense that it spawns its own relay, but it still `require`s `ws`, which is not vendored. Without the install the relay job fails on a bare runner. `package-lock.json` is tracked, so `ci` (not `install`) is the correct verb, and `cache: npm` keyed on that lockfile keeps it fast. No new dependency — `ws` was already the relay's only one.
  - BUMPED actions/checkout AND actions/setup-node FROM @v4 TO @v5 after the first run. The v4 majors work, but the runners now force their Node 20 runtime onto Node 24 and annotate every single run about the deprecation. Left alone, GitHub's calendar — not our diff — eventually turns this repo red, which is the same failure mode the exact Flutter pin exists to prevent. The green run above carries zero annotations.
  - The bite-check branch was merged forward (a normal merge commit, no force-push) so both proof runs below exercise the identical workflow file.
  - No README badge (T4.5), no apk build, no secrets, and `permissions: contents: read` — the built-in GITHUB_TOKEN is read-only to this workflow.
  - Untouched, as instructed: the pre-existing uncommitted leftovers in the working tree (docs/GIT_SURVIVAL_GUIDE.md and the untracked M1–M3 tickets/reports) are not in this branch.

ACTIONS RUNS (the asymmetric acceptance — one green AND one red):
  - GREEN · m4/ci — https://github.com/ponder-the-orb/skate_p2p/actions/runs/33931643645
    flutter ✅ (`🎉 172 tests passed.`) · relay ✅ (`🎉 ALL TESTS PASSED!`)
  - RED · m4/ci-bite-check — https://github.com/ponder-the-orb/skate_p2p/actions/runs/33931649368
    flutter ❌ (`172 tests passed, 1 failed.` → `Process completed with exit code 1`) · relay ✅
    The red job fails at the `flutter test` step with analyze and format already green, and the relay job stays green beside it — so a failing test reads as a failing test, and a relay break would read as a relay break.

THROWAWAY BRANCH: m4/ci-bite-check (one commit adding test/ci_bite_check_test.dart, `expect(true, false)`, plus the merge-forward commit). Never opened as a PR, never merged — for the Producer to delete at the next sweep.

DIFF: https://github.com/ponder-the-orb/skate_p2p/pull/15
