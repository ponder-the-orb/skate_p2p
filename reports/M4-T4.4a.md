REPORT M4-T4.4a — DONE

CHANGED:
- `tickets/M4-T4.4a.md` (new, +74)
- `lib/ui/build_info.dart` (new, +12)
- `lib/ui/screens/lobby_screen.dart` (+42/-0)
- `lib/core/state/app_state.dart` (+25/-4)
- `lib/main.dart` (+4/-2)
- `test/build_info_test.dart` (new, +13)
- `test/lobby_screen_test.dart` (+39/-2)
- `test/app_state_test.dart` (+48/-1)
- `test/packet_dispatcher_test.dart` (+2/-2)
- `CHANGELOG.md` (+11/-0)
- `reports/M4-T4.4a.md` (new, this file)

CHECKS: analyze ✅  test ✅ (215 passed)  format ✅  rooms_smoke ➖ (zero relay diff)

COMMITS:
- `docs: ticket M4-T4.4a`
- `feat(ui): appVersion, baked in the way relayUrl is`
- `feat(ui): the lobby says what build it is and offers a Retry when dead`
- `feat(state): room errors that read like a person wrote them`
- `fix(ui): a documents directory that never answers turns Rejoin off, not the app`
- `docs: changelog for M4-T4.4a`
- `docs: report for M4-T4.4a`

CHANGELOG: updated — `[Unreleased] → M4-T4.4a`

QUESTIONS/FLAGS:
- **Stacked, as the ticket's own check instructed.**
  `git merge-base --is-ancestor origin/m4/rejoin origin/main` answered
  **no**, so the branch is cut from `origin/m4/rejoin` and PR #17's base is
  `m4/rejoin`. Producer retargets to `main` once #16 merges.
- **One file outside the ticket's FILES list: `test/packet_dispatcher_test.dart`.**
  Two of its assertions pinned the old copy verbatim (`'Room full'`,
  `'Room not found'`), so changing the copy could not leave them green. Both
  now compare against the new named constants — same assertions, no
  restructuring, +2/-2. Flagging it because the ticket's FILES list did not
  name it.
- **The copy lives as three named declarations, not three inline literals.**
  `roomFullMessage`, `roomNotFoundMessage` and `unknownErrorMessage(int)` at
  the top of `app_state.dart`. The strings are byte-for-byte what the ticket
  specified; naming them is so the tests assert *the* copy rather than a
  second hand-typed copy of it, which is how copy drifts. One test does
  spell all three out verbatim, deliberately, as the place the ticket's
  wording is pinned.
- **The RETRY trap did not bite the way the ticket expected.** The existing
  `lobby_screen_test.dart` helper pumps `setConnectionStatus(true)`, so every
  pre-existing lobby test is *connected* and none of them grew a RETRY. The
  helper gained a `connected` flag rather than a second helper; no finder or
  count in an existing test needed changing, and none was restructured. The
  version footer is a new `Text` in every tree, but nothing asserts an exact
  `Text` count, so it too passed unmodified.
- **`handlePeerLeft` clears on the engine's verdict, not on the opcode.**
  The clear is guarded by `_game.phase == GamePhase.abandoned` after the
  event is applied, so it fires on the fact the engine agrees happened. The
  no-store-attached test now calls `handlePeerLeft` too, keeping the "every
  hook is null-safe" case complete.
- `signaling_service.dart` untouched, per the ticket.
- Manual pass still owed (Producer): `v dev` in the lobby; kill the relay →
  RETRY appears and reconnects when the relay returns; a wrong code shows the
  new copy.

DIFF: https://github.com/ponder-the-orb/skate_p2p/pull/17
